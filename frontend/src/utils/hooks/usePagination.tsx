import { useState } from 'react';

export const usePagination = (currentPage: number, count: number, limit: number) => {
  // Unsure how to actually use this, since we don't want to just set state, we actually want to link
};
